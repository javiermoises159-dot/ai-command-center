{
  "name": "@acc/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@acc/contracts": "workspace:*",
    "@acc/database": "workspace:*",
    "@acc/domain": "workspace:*",
    "@acc/madre": "workspace:*",
    "@acc/orchestrator": "workspace:*",
    "@acc/providers": "workspace:*",
    "@acc/repositories": "workspace:*",
    "express": "^5.1.0",
    "pg": "^8.16.0"
  },
  "//": "`pg` is declared here even though the import lives in @acc/database: the production bundle externalises it (native bindings cannot be bundled), so it must resolve from apps/server/node_modules at runtime.",
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
