{
  "//": "Server and packages. The web app is a separate project (apps/web/tsconfig.json) because it needs DOM + vite/client types that the backend must not silently depend on. `pnpm typecheck` runs both.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["packages/*/src/**/*.ts", "apps/server/src/**/*.ts"],
  "exclude": ["node_modules", "**/dist/**"]
}
